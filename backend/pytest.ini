[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = src
addopts = -q --strict-markers
asyncio_mode = auto
norecursedirs = _tmp_ml_backtesting pytest-cache-files-* .codex_tmp .git __pycache__ *.egg-info
markers =
    asyncio: mark test as async (handled by pytest-asyncio)
