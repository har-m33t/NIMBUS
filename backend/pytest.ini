[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = src
addopts = -q --strict-markers
asyncio_mode = auto
norecursedirs = _tmp_* .git __pycache__ *.egg-info
markers =
    asyncio: mark test as async (handled by pytest-asyncio)
